export default (theme) => ({
  root: {
    padding: 10,
  },

  event: {
    marginBottom: 10,
  },

  pagination: {
    display: 'flex',
    justifyContent: 'center',
  },

  link: {
    color: theme.palette.primary2Color,
  }
});
